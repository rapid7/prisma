/**
 * @constant {number} BRIGHTNESS_THRESHOLD
 */
export const BRIGHTNESS_THRESHOLD = 130;

/**
 * @constant {Array<number>} BRIGHTNESS_THRESHOLD
 */
export const DARK_TEXT_MULTIPLIERS = [0.241, 0.691, 0.068];

/**
 * @constant {Array<number>} BRIGHTNESS_THRESHOLD
 */
export const DARK_TEXT_W3C_ADDITIVE = [0.2126, 0.7152, 0.0722];

/**
 * @constant {string} DEFAULT_HEX_CODE_VALUE
 */
export const DEFAULT_HEX_CODE_VALUE = '#000000';

/**
 * @constant {number} L_THRESHOLD
 */
export const L_THRESHOLD = Math.sqrt(1.05 * 0.05) - 0.05;

/**
 * @constant {RegExp} SHORTHAND_REGEXP
 */
export const SHORTHAND_REGEXP = /^#?([a-f\d])([a-f\d])([a-f\d])$/i;
